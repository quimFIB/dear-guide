---
description: What could be picked up right now — decidable questions and ready work
allowed-tools: Bash(dg show:*), Bash(dg task:*)
---

Questions that are not settled:

!`dg show`

Work outstanding, and what is startable:

!`dg task`

Read the two together: a decision with work resting on it is one whose answer
somebody is already waiting for, and a task whose premise is unsettled is not
ready however clear its own prerequisites are.

Titles are clipped to one line each. Re-run either with `--full` if a title is
ambiguous, or `dg context <id>` for the reasoning behind one of them.

$ARGUMENTS
