# A remit is one word. Everything else in the prompt comes from the graph.
$ dg-agent presets
                      what it settles      $DG_DECIDE  $DG_APPLY            may run unasked     
scout                 proposes only ·      never       never  you apply     17 programs  readers
                      nothing lands                                         only                
                      unapproved                                                                
contributor           settles what         evidence    own  applies its     18 programs  + build
(default)             evidence backs                   own                  tools               
maintainer            settles anything     open        own  applies its     18 programs  + build
                                                       own                  tools               

The same in all three: $DG_WRITE=launch, $DG_AREA=open, $DG_TERSE=on, and a confinement floor 
where one is available. None of them sets a budget — that follows the size of the work, not the 
remit.
`dg-agent setup --preset <name>`, or pick one in the wizard; any other flag still overrides one 
field.
# What can be worked on at once: the set setup will assign, one task per agent.
$ dg task independent
INDEPENDENT  3 of 3 ready can run side by side
  T04  Detect a corrupted index on open
  T05  Fix path escaping noticed while writing the loader
  T11  Resolve wikilinks in search output
$ dg-agent setup --preset contributor --focus D06 --agents 2 --budget 20m --brief 'Settle what happens when the index is corrupted, on evidence rather than opinion.' --read 'docs/index-format.md:how the index file is laid out' --findings 'findings/<task-id>.md'
✓ decisions.json exists
✓ tasks.json exists
✓ 7004 agent names free
✓ 3 task(s) ready to claim: T04, T05, T11
✓ 3 of those can run side by side: T04, T05, T11
! no consent broker — an escalation will be refused, not asked  dg-agent broker (a terminal), or
`--relay --plan fanout/env.json` to answer from a session
· assigned T04, T05 — one agent each; no two name a decision the other would move
· 1 more could run beside these: T11
wrote fanout/scout.md
wrote fanout/launch.sh
wrote fanout/env.json
read fanout/scout.md before launching — the three answers only you can give are near the top. 
Then start a broker — `dg-agent broker`, or `--relay` to answer from a session — and 
`./fanout/launch.sh`
$ ls fanout
env.json
launch.sh
scout.md
$ sed -n 1,40p fanout/scout.md
You are one of several agents working on notelit through a shared decision
graph. Other agents are working at the same time, in the same checkout, staging
into the same tray. Your name is in `$DG_AGENT`; every op you stage is stamped
with it.

## What you are here to do

Settle what happens when the index is corrupted, on evidence rather than opinion.

## Why this work exists

D06  OPEN  What happens when the index is corrupted?  [storage]
  rests on D04

RESTS ON (3) — nearest premise last
  D01  DECIDED     What is the note store format?
       Plain markdown files on disk, one note per file. The folder is the
       database.
       falsifier:
         a user needs attachments larger than 10 MB inside a note, or one
         note is edited from two machines at once
       source: discussion  ·  2026-05-04
       also opened: D02, D05, D08
  D02  DECIDED     How are notes indexed for search?
       SQLite FTS5 with a trigram tokenizer, one table for every notebook.
       falsifier:
         p95 over 200 ms at 100k notes
       source: bench/fts5-trigram.md  ·  2026-07-10
       also opened: D03, D04
  D04  DECIDED     Where does the index live?
       Beside the notes, in .notelit/index.db. Rebuilt on demand, never
       synced.
       falsifier:
         users sync the notes folder and the index file causes conflicts
       source: discussion  ·  2026-06-15

WORK
  because of this: T09
  evidence for it: T04*
  (* not finished yet)
$ cat fanout/env.json
{
  "decide": "evidence",
  "apply": "own",
  "write": "launch",
  "area": "open",
  "terse": "on",
  "budget": 1200,
  "exec_allow": [
    "dg",
    "dg-agent",
    "ls",
    "cat",
    "head",
    "tail",
    "wc",
    "grep",
    "rg",
    "diff",
    "file",
    "stat",
    "which",
    "pwd",
    "echo",
    "basename",
    "dirname",
    "git"
  ],
  "confine": "require",
  "floor": "host"
}
$ cat fanout/launch.sh
#!/usr/bin/env bash
# Generated by `dg-agent setup`. Re-run it to regenerate.
#
# The remit is in fanout/env.json, which is also what
# fanout/scout.md was rendered from — so the prompt's claims about
# what each agent may do and what this actually sets cannot drift.
# Change a policy there, not here.
set -euo pipefail

cd "$(dirname "$0")/.."

# Before any agent starts: every value in the plan is one this `dg`
# understands, and nothing exported in this shell contradicts it.
dg-agent env --check --plan fanout/env.json

# ...and whether anybody is there to answer an escalation. A warning,
# never a refusal: a run with no broker is legal and behaves exactly as
# it did before one existed -- the gate returns its own verdict, and an
# `ask` becomes a refusal. What is not legal is finding that out from
# an agent that stopped. `agentic/QUICKSTART.md` calls this the step
# people skip, and until now only prose said so. `D53`.
dg-agent broker --check || true

# One agent per task, named. `$DG_TASK` reaches the agent through
# `dg-agent run`, which sets it for the child only -- the same
# rule as `$DG_AGENT`, and for the same reason: an exported
# assignment would make this launcher an agent holding work.
for t in T04 T05; do
  dg-agent run --floor-applied --plan fanout/env.json --task "$t" \
    -- claude --settings '{"sandbox":{"enabled":true,"autoAllowBashIfSandboxed":true,"allowUnsandboxedCommands":false,"filesystem":{"allowWrite":["/tmp","/tmp/dg-quick-start/notelit"],"denyWrite":["/tmp/dg-quick-start/notelit/decisions.json","/tmp/dg-quick-start/notelit/tasks.json"]}}}' -p "$(cat fanout/scout.md)" &
done

dg-agent list      # who holds what, time left, and who has gone quiet
wait
# `dg-agent run` already parked what a child of *this* script dropped.
# This is the backstop for what it cannot see: the script itself being
# killed, the machine going down, the terminal closing.
dg-agent expire
dg pending         # the tray: who proposed what
$ dg-agent env --check --plan fanout/env.json
variable         set to  effective                     read as                                  
DG_AGENT         —       —                             supervisor — no refusal applies          
DG_TASK          —       —                             nothing is assigned; read the frontier   
DG_DECIDE        —       open                          may close any question                   
DG_WRITE         —       open                          anywhere                                 
DG_APPLY         —       own                           writes its own staged ops itself         
DG_EXEC_ALLOW    —       nothing — every command asks  every command asks                       
DG_CONFINE       —       off                           no floor — the gate and the broker are   
                                                       all that judge                           
DG_FLOOR         —       host                          host — usable here                       
DG_AREA          —       open                          any area; a new one is checked against   
                                                       the ones in use                          
DG_BUDGET        —       infinite                      infinite                                 
DG_TERSE         —       no limit                      no limit on a field                      
DG_SILENT_AFTER  —       15m                           default                                  
DG_PROJECT       —       /tmp/dg-quick-start/notelit   nearest ancestor                         
every variable set here was understood
# A roster written by hand is obeyed as written; a pair that may collide is said, not refused.
$ dg task add --id T13 --area storage --title 'Measure how often an index corrupts in practice' --evidence-for D06
staged add T13
$ dg-agent setup --preset contributor --roster T04,T13 --budget 20m --brief 'Settle what happens when the index is corrupted, on evidence rather than opinion.' --findings 'findings/<task-id>.md' --dry-run
✓ decisions.json exists
✓ tasks.json exists
✓ 7004 agent names free
✓ 4 task(s) ready to claim: T04, T05, T11, T13
✓ 3 of those can run side by side: T04, T05, T11
! no consent broker — an escalation will be refused, not asked  dg-agent broker (a terminal), or
`--relay --plan fanout/env.json` to answer from a session
! T04 and T13 may collide: both name D06, and each would move it
… 230 more line(s)
# The other flow: the session spawns the agents itself. Same prompt; the rules become advisory, and setup says which.
$ dg-agent setup --mode session --preset contributor --focus D06 --agents 2 --budget 20m --brief 'Settle what happens when the index is corrupted, on evidence rather than opinion.' --findings 'findings/<task-id>.md' --dry-run
✓ decisions.json exists
✓ tasks.json exists
✓ 7004 agent names free
✓ 4 task(s) ready to claim: T04, T05, T11, T13
✓ 3 of those can run side by side: T04, T05, T11
! no consent broker — an escalation will be refused, not asked  dg-agent broker (a terminal), or
`--relay --plan fanout/env.json` to answer from a session
· assigned T04, T05 — one agent each; no two name a decision the other would move
· 1 more could run beside these: T11
! mode session: the agents are spawned by the session, so these are advisory here
    · $DG_AGENT — a subagent shares the session's environment, so without a `DG_AGENT=<name>` 
prefix on every call it reads as the supervisor (D32)
    · the confinement floor — no `dg-agent run` parent to prepend it (D33)
    · the budget — the same; `$DG_BUDGET` is advisory and `dg-agent expire` the only backstop 
(D33)
    · $DG_TASK — not lost but relocated: the setup report prints the roster, so the session 
hands each agent its task in the spawn instructions instead (D61)
    · a relayed verdict's claim to `person` — the channel cannot prove a person wrote it with no
floor (D40)
    roster T04, T05 — one agent per task, in this order: name each agent's task in its spawn 
instructions; there is no `$DG_TASK` here
    this mode is the plugin's workflow (`/dg:fanout`); from a terminal nothing refuses it, but 
… 261 more line(s)
# Turning a proposal down leaves the others where they are.
$ b=$(dg-agent claim)
agile-azimuth
agile-azimuth ▸ $ dg add --id D09 --area ux --title 'Should search paginate?' --after D05
staged add D09
$ dg pending
STAGED  2 op(s)
  0  qwkq  add_vertex  D09  Should search paginate? [ux]   ·  by agile-azimuth
  1  ywzh  add_edge    D05  → D09                           ·  by agile-azimuth
staged by  agile-azimuth 2
  `dg pending --agent <name>` for one writer alone
`dg apply` to write, `dg drop <id>` to unstage
  `dg pending --full` for the table, nothing clipped
$ dg clear --agent agile-azimuth
cleared 2 op(s) staged by agile-azimuth
$ dg pending
nothing staged
