# A second piece of evidence for D06, beside T04: two tasks that would move the same decision.
$ dg task add --id T13 --area storage --title 'Measure how often an index corrupts in practice' --evidence-for D06
staged add T13
$ dg task independent
INDEPENDENT  3 of 4 ready can run side by side
  T04  Detect a corrupted index on open
  T05  Fix path escaping noticed while writing the loader
  T11  Resolve wikilinks in search output
held
  T13 cannot join: shares D06 with T04
# setup assigns each agent one task from that set. Four agents asked for, three independent tasks: three launch.
$ dg-agent setup --preset contributor --agents 4 --budget 20m --brief 'Settle what happens when the index is corrupted, on evidence rather than opinion.' --findings 'findings/<task-id>.md' --dry-run
✓ decisions.json exists
✓ tasks.json exists
✓ 7004 agent names free
✓ 4 task(s) ready to claim: T04, T05, T11, T13
✓ 3 of those can run side by side: T04, T05, T11
! no consent broker — an escalation will be refused, not asked  dg-agent broker (a terminal), or
`--relay --plan fanout/env.json` to answer from a session
· assigned T04, T05, T11 — one agent each; no two name a decision the other would move
· 4 agents asked for, 3 independent task(s) ready: launching 3
… 231 more line(s)
$ a=$(dg-agent claim)
agile-azimuth
$ b=$(dg-agent claim)
agile-bearing
agile-azimuth ▸ $ dg task start T04
staged T04 → DOING
agile-azimuth ▸ $ dg apply --mine
✓ applied 1 op(s) → tasks.json; the view is regenerated on demand with `dg task render`
agile-bearing ▸ $ dg add --id D09 --area sync --title 'Which files does sync ignore?' --after D08
staged add D09
agile-bearing ▸ $ dg task add --id T12 --area sync --title 'List the files sync must ignore' --because D08 --after T08
staged add T12
staged T12 after T08
$ dg pending
STAGED  2 op(s)
  0  zadk  add_vertex  D09  Which files does sync ignore? [sync]   ·  by agile-bearing
  1  kytt  add_edge    D08  → D09                                   ·  by agile-bearing
staged by  agile-bearing 4
  `dg pending --agent <name>` for one writer alone
`dg apply` to write, `dg drop <id>` to unstage
  `dg pending --full` for the table, nothing clipped
$ dg apply
✗ nothing written — 4 staged op(s) belong to agile-bearing
`dg apply --all` writes theirs too, `dg apply --mine` only yours, `dg apply --agent <name>` one 
of them; `dg pending` shows who staged what
[exit 1]
$ dg pending --agent agile-bearing
STAGED  2 op(s)
  0  zadk  add_vertex  D09  Which files does sync ignore? [sync]   ·  by agile-bearing
  1  kytt  add_edge    D08  → D09                                   ·  by agile-bearing
staged by  agile-bearing 4  — showing agile-bearing
`dg apply` to write, `dg drop <id>` to unstage
  `dg pending --full` for the table, nothing clipped
$ dg apply --agent agile-bearing
✓ applied 2 op(s) → decisions.json; the view is regenerated on demand with `dg render`
✓ applied 2 op(s) → tasks.json; the view is regenerated on demand with `dg task render`
$ dg-agent list
                           Names held                            
┏━━━━━━━━━━━━━━━┳━━━━━━━━━━━━┳━━━━━━━━┳━━━━━━━━━┳━━━━━━━━┳━━━━━━┓
┃ Name          ┃ Since      ┃ Staged ┃ Holding ┃ Budget ┃ Seen ┃
┡━━━━━━━━━━━━━━━╇━━━━━━━━━━━━╇━━━━━━━━╇━━━━━━━━━╇━━━━━━━━╇━━━━━━┩
│ agile-azimuth │ 2026-09-02 │ 0      │ T04     │ —      │ 7s   │
│ agile-bearing │ 2026-09-02 │ 0      │ —       │ —      │ 5s   │
└───────────────┴────────────┴────────┴─────────┴────────┴──────┘
7002 of 7004 names free
