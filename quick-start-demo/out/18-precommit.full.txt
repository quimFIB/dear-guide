$ dg add --id D09 --area sync --title 'Which edit wins when two machines change one note?' --after D08 --rule 'a week of the team'\''s own notes syncing with no edit lost' --probe '{"kind": "prose.rule", "args": {"text": "seven days, zero lost edits"}}'
staged add D09
$ dg apply
✓ applied 2 op(s) → decisions.json; the view is regenerated on demand with `dg render`
# A rule is appended, never edited: the earlier one stays, dated, so a rule rewritten to fit the evidence is visible as one.
$ dg reprobe D09 --probe '{"kind": "prose.rule", "args": {"text": "seven days, zero lost edits, on three machines"}}'
staged reprobe D09 — entry 2; the earlier 1 stay
$ dg apply
✓ applied 1 op(s) → decisions.json; the view is regenerated on demand with `dg render`
# What the question is about, in some domain's terms. An address, not a claim; it accumulates like an edge.
$ dg bind D09 notelit.module:sync
staged D09 bound to notelit.module:sync
$ dg apply
✓ applied 1 op(s) → decisions.json; the view is regenerated on demand with `dg render`
$ dg node D09
╭──────────────────────────────────────────── D09 ─────────────────────────────────────────────╮
│ Which edit wins when two machines change one note?                                           │
│                                                                                              │
│ status      OPEN                                                                             │
│ area        sync                                                                             │
│ depends on  D08                                                                              │
│ opens       —                                                                                │
│ bound to    notelit.module:sync                                                              │
│                                                                                              │
│ Rule for settling                                                                            │
│ a week of the team's own notes syncing with no edit lost                                     │
│                                                                                              │
│ Rule for settling (probe)                                                                    │
│   2026-09-03  prose.rule {"text": "seven days, zero lost edits"}                             │
│   2026-09-03  prose.rule {"text": "seven days, zero lost edits, on three machines"}  (live)  │
╰──────────────────────────────────────────────────────────────────────────────────────────────╯
# The falsifier's twin travels with the answer, and is archived with it if the answer is ever reopened.
$ dg decide D08 --answer 'The notes folder is synced by whatever the user already uses; notelit never syncs.' --source discussion --falsifier 'a user asks for notelit to resolve a conflict it did not cause' --probe '{"kind": "prose.falsifier", "args": {"text": "a conflict notelit did not cause"}}'
╭──────────────────────────────────────────── D08 ─────────────────────────────────────────────╮
│ How do two machines share one notes folder?                                                  │
│                                                                                              │
│ depends on D01                                                                               │
╰──────────────────────────────────────────────────────────────────────────────────────────────╯
staged 1 op(s) — review with `dg pending`, then `dg apply`
$ dg apply
✓ applied 1 op(s) → decisions.json; the view is regenerated on demand with `dg render`
# The shape is checked at the door, before anything is asked for.
$ dg reprobe D09 --probe '{"kind": "nodot", "args": {}}'
--probe: a probe's kind is <domain>.<name> — 'nodot' does not name a domain
[exit 1]
# A kind no installed domain claims is presented, not evaluated, and never an error: a plain install must still read a graph written by a richer one.
$ dg reprobe D09 --probe '{"kind": "rocq.no_admit", "args": {"lemma": "sync_merge_total"}}'
staged reprobe D09 — entry 3; the earlier 2 stay
$ dg apply
✓ applied 1 op(s) → decisions.json; the view is regenerated on demand with `dg render`
$ dg probe --area sync
D08  DECIDED  How do two machines share one notes folder?
    falsifier         a user asks for notelit to resolve a conflict it did not cause
    probe             prose.falsifier {"text": "a conflict notelit did not cause"}
    verdict           unjudged  a conflict notelit did not cause

D09  OPEN  Which edit wins when two machines change one note?
    rule for settling a week of the team's own notes syncing with no edit lost
    probe             rocq.no_admit {"lemma": "sync_merge_total"}  (2026-09-03)
    verdict           unjudged  no installed domain claims `rocq.` — nothing registered under 
`dgraph.domains`

2 presented · fired 0 · holds 0 · unjudged 2
# A reopened premise: every decision resting on it is presented beside what moved, by the dates, labelled as the heuristic it is.
$ dg reopen D02 --why 'the trigram tokenizer misses CJK notes entirely' --yes
╭───────────────────────────────────────── reopen D02 ─────────────────────────────────────────╮
│ How are notes indexed for search?                                                            │
│                                                                                              │
│ Its answer becomes superseded; its dependencies stay.                                        │
│                                                                                              │
│ 1 decided descendant(s) rest on it and become PROVISIONAL:                                   │
│   D04                                                                                        │
│                                                                                              │
│ 1 unfinished task(s) rest on a premise under review:                                         │
│   T10                                                                                        │
╰──────────────────────────────────────────────────────────────────────────────────────────────╯
staged 2 op(s)
$ dg apply
✓ applied 2 op(s) → decisions.json; the view is regenerated on demand with `dg render`
$ dg probe --provisional
D04  PROVISIONAL  Where does the index live?
    falsifier         users sync the notes folder and the index file causes conflicts
    verdict           unjudged
    still under review: D02
    settled again since this was decided (by the dates, a heuristic):
      D02 REOPENED: was “SQLite FTS5 with a trigram tokenizer, one table for every notebook.” → 
now “—”

1 presented · fired 0 · holds 0 · unjudged 1
# The check never evaluates a probe. Its verdict is a function of the store alone, which is what keeps a commit hook honest on a machine with nothing installed.
$ dg check
! [stale_view] (warning) decision-graph.md does not match decisions.json. It is generated — run 
`dg render` rather than editing it.
! [evidence_unharvested] (warning) T02 is DONE and was to inform D02 (How are notes indexed for 
search?), which is still unsettled — record what it showed with `dg decide D02`, or drop the 
link
! [link_premise_under_review] (warning) T10 is PARKED and exists because of D02, which is 
REOPENED — the reason for this work is under review; re-check it before carrying on
✓ 9 vertices, 8 edges; 11 tasks, all invariants hold, 3 warning(s)
