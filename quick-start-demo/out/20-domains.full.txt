$ dg add --id D09 --area search --title 'Does search run as you type?' --after D02
staged add D09
$ dg decide D09 --answer 'Yes: every keystroke runs a search. Both machines answer in under 100 ms.' --source 'bench/server.md, bench/laptop.md' --falsifier 'a search p95 of 100 ms or more on the build server, or on the slowest team laptop' --probe '{"kind": "core.all_of", "args": {"probes": [{"kind": "grep.matches", "args": {"file": "bench/server.md", "pattern": "^p95: [0-9]{3,} ms"}}, {"kind": "grep.matches", "args": {"file": "bench/laptop.md", "pattern": "^p95: [0-9]{3,} ms"}}]}}'
╭──────────────────────────────────────────── D09 ─────────────────────────────────────────────╮
│ Does search run as you type?                                                                 │
│                                                                                              │
│ depends on D02                                                                               │
╰──────────────────────────────────────────────────────────────────────────────────────────────╯
staged 1 op(s) — review with `dg pending`, then `dg apply`
# A second answer resting on two things, and only one of them is a file. Ranking by recency stands while the laptop stays under 100 ms and the ranking test keeps passing, and that test needs a pytest domain this machine does not have.
$ dg add --id D10 --area search --title 'Are results ranked by how recently a note changed?' --after D09
staged add D10
$ dg decide D10 --answer 'Yes: recently changed notes first.' --source 'bench/laptop.md, tests/test_rank.py' --falsifier 'a search p95 of 100 ms or more on the slowest team laptop, or the recency ranking test failing' --probe '{"kind": "core.all_of", "args": {"probes": [{"kind": "grep.matches", "args": {"file": "bench/laptop.md", "pattern": "^p95: [0-9]{3,} ms"}}, {"kind": "pytest.fails", "args": {"node": "tests/test_rank.py::test_recent_first"}}]}}'
╭──────────────────────────────────────────── D10 ─────────────────────────────────────────────╮
│ Are results ranked by how recently a note changed?                                           │
│                                                                                              │
│ depends on D09                                                                               │
╰──────────────────────────────────────────────────────────────────────────────────────────────╯
staged 1 op(s) — review with `dg pending`, then `dg apply`
$ dg apply
✓ applied 6 op(s) → decisions.json; the view is regenerated on demand with `dg render`
# On every commit, only the cheap checks: the ones the grep domain runs. D10 comes up too, because half of it is a grep and a probe is one condition. Its other half nobody here can check, so D10 is unjudged, never passed, and the missing domain is named once at the bottom.
$ dg probe --domain grep
D09  DECIDED  Does search run as you type?
    falsifier         a search p95 of 100 ms or more on the build server, or on the slowest team
laptop
    probe             core.all_of {"probes": [{"args": {"file": "bench/server.md", "pattern": 
"^p95: [0-9]{3,} ms"}, "kind": "grep.matches"}, {"args": {"file": "bench/laptop.md", "pattern": 
"^p95: [0-9]{3,} ms"}, "kind": "grep.matches"}]}
    verdict           holds  no line of bench/server.md matches /^p95: [0-9]{3,} ms/; no line of
bench/laptop.md matches /^p95: [0-9]{3,} ms/

D10  DECIDED  Are results ranked by how recently a note changed?
    falsifier         a search p95 of 100 ms or more on the slowest team laptop, or the recency 
ranking test failing
    probe             core.all_of {"probes": [{"args": {"file": "bench/laptop.md", "pattern": 
"^p95: [0-9]{3,} ms"}, "kind": "grep.matches"}, {"args": {"node": 
"tests/test_rank.py::test_recent_first"}, "kind": "pytest.fails"}]}
    verdict           unjudged  no line of bench/laptop.md matches /^p95: [0-9]{3,} ms/; no 
installed domain claims `pytest.` — nothing registered under `dgraph.domains`

2 presented · fired 0 · holds 1 · unjudged 1
`pytest.` not judged here — 1 record(s): no installed domain claims `pytest.` — nothing 
registered under `dgraph.domains`
# In the nightly job, where the test plugin would be installed, only the checks that need it. Here it is not, so D10 stays unjudged.
$ dg probe --domain pytest
D10  DECIDED  Are results ranked by how recently a note changed?
    falsifier         a search p95 of 100 ms or more on the slowest team laptop, or the recency 
ranking test failing
    probe             core.all_of {"probes": [{"args": {"file": "bench/laptop.md", "pattern": 
"^p95: [0-9]{3,} ms"}, "kind": "grep.matches"}, {"args": {"node": 
"tests/test_rank.py::test_recent_first"}, "kind": "pytest.fails"}]}
    verdict           unjudged  no line of bench/laptop.md matches /^p95: [0-9]{3,} ms/; no 
installed domain claims `pytest.` — nothing registered under `dgraph.domains`

1 presented · fired 0 · holds 0 · unjudged 1
`pytest.` not judged here — 1 record(s): no installed domain claims `pytest.` — nothing 
registered under `dgraph.domains`
# A domain that hangs must not hang the job. Each one runs in its own process under a deadline: grep declares five seconds, the default is sixty, and --timeout overrides both.
$ dg probe D09 --timeout 0.001
D09  DECIDED  Does search run as you type?
    falsifier         a search p95 of 100 ms or more on the build server, or on the slowest team
laptop
    probe             core.all_of {"probes": [{"args": {"file": "bench/server.md", "pattern": 
"^p95: [0-9]{3,} ms"}, "kind": "grep.matches"}, {"args": {"file": "bench/laptop.md", "pattern": 
"^p95: [0-9]{3,} ms"}, "kind": "grep.matches"}]}
    verdict           unjudged  `grep` did not answer within 0.001s; `grep` did not answer 
within 0.001s

1 presented · fired 0 · holds 0 · unjudged 1
# A script that names a domain nothing uses, or passes an empty name because a variable was unset, is refused rather than run as check-nothing or check-everything.
$ dg probe --domain rocq
no pre-commitment is under `rocq.`; prefixes here: grep, prose, pytest
[exit 2]
$ dg probe --domain ''
Usage: dg probe [OPTIONS] [ID...]
Try 'dg probe --help' for help.
╭─ Error ──────────────────────────────────────────────────────────────────────────────────────╮
│ Invalid value for '--domain': a blank name or ref is a selection that matched nothing, not   │
│ the whole tray, and is refused rather than widened                                           │
╰──────────────────────────────────────────────────────────────────────────────────────────────╯
[exit 2]
